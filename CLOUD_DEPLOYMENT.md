# ☁️ Деплой на облачные платформы

## Содержание
- [Render.com (Рекомендуется для начинающих)](#rendercom)
- [Heroku](#heroku)
- [Railway.app](#railwayapp)
- [VPS Провайдеры](#vps-провайдеры)

---

## Render.com

### Преимущества:
- ✅ Бесплатный план
- ✅ Простая настройка
- ✅ Автоматический деплой из Git
- ✅ Встроенные логи

### Инструкция:

**1. Подготовьте репозиторий**

```bash
# Создайте Git репозиторий (GitHub, GitLab, etc.)
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push -u origin main
```

**2. Зарегистрируйтесь на [Render.com](https://render.com)**

**3. Создайте новый Worker**

1. Dashboard → "New +" → "Worker"
2. Подключите ваш Git репозиторий
3. Настройте:
   - **Name**: `arctbot`
   - **Region**: Frankfurt (ближе к Европе)
   - **Branch**: `main`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`

**4. Добавьте переменные окружения**

В разделе "Environment Variables":
```
BOT_TOKEN = ваш_токен_от_BotFather
SPREADSHEET_ID = 1YYvqtrrEG2ssNLbKnsIX3goVQfpeJ-E8wcM06P2ts7Q
LOG_LEVEL = INFO
```

**5. Загрузите Google Credentials**

1. В настройках сервиса найдите "Secret Files"
2. Нажмите "Add Secret File"
3. **Filename**: `config/google_credentials.json`
4. **Contents**: Вставьте содержимое вашего `google_credentials.json`
5. Сохраните

**6. Запустите**

Нажмите "Create Worker" - Render автоматически:
- Установит зависимости
- Запустит бота
- Покажет логи

**7. Мониторинг**

- Логи доступны во вкладке "Logs"
- Перезапуск: "Manual Deploy" → "Deploy latest commit"

---

## Heroku

### Преимущества:
- ✅ Популярная платформа
- ✅ Простой CLI
- ✅ Много аддонов

⚠️ **Примечание**: Бесплатный план Heroku больше не доступен (с ноября 2022)

### Инструкция:

**1. Установите Heroku CLI**

```bash
# Windows
# Скачайте с https://devcenter.heroku.com/articles/heroku-cli

# Mac
brew tap heroku/brew && brew install heroku

# Linux
curl https://cli-assets.heroku.com/install.sh | sh
```

**2. Авторизуйтесь**

```bash
heroku login
```

**3. Создайте приложение**

```bash
# В папке проекта
heroku create arctbot

# Добавьте buildpack для Python
heroku buildpacks:set heroku/python
```

**4. Добавьте переменные окружения**

```bash
heroku config:set BOT_TOKEN="ваш_токен"
heroku config:set SPREADSHEET_ID="1YYvqtrrEG2ssNLbKnsIX3goVQfpeJ-E8wcM06P2ts7Q"
heroku config:set LOG_LEVEL="INFO"
```

**5. Загрузите Google Credentials**

⚠️ **Важно**: Heroku не поддерживает загрузку файлов напрямую.

**Решение 1**: Храните credentials в переменной окружения (не рекомендуется)
```bash
# НЕ РЕКОМЕНДУЕТСЯ - только для тестирования
heroku config:set GOOGLE_CREDENTIALS="$(cat config/google_credentials.json)"
```

**Решение 2**: Используйте Google Cloud Secret Manager или другой сервис

**Решение 3**: Используйте Render.com вместо Heroku

**6. Деплой**

```bash
git add .
git commit -m "Deploy to Heroku"
git push heroku main

# Запустите worker
heroku ps:scale worker=1
```

**7. Проверка**

```bash
# Логи
heroku logs --tail

# Статус
heroku ps
```

---

## Railway.app

### Преимущества:
- ✅ Простой интерфейс
- ✅ Бесплатный план ($5/месяц кредитов)
- ✅ Деплой из GitHub

### Инструкция:

**1. Зарегистрируйтесь на [Railway.app](https://railway.app)**

**2. Создайте новый проект**

1. "New Project" → "Deploy from GitHub repo"
2. Выберите ваш репозиторий
3. Railway автоматически определит Python проект

**3. Настройте переменные**

В разделе "Variables":
```
BOT_TOKEN = ваш_токен
SPREADSHEET_ID = 1YYvqtrrEG2ssNLbKnsIX3goVQfpeJ-E8wcM06P2ts7Q
```

**4. Загрузите credentials**

К сожалению, Railway также не поддерживает загрузку файлов напрямую.
Рекомендуется использовать Render.com или VPS.

**5. Деплой**

Railway автоматически делает деплой при push в GitHub.

---

## VPS Провайдеры

Для полного контроля рекомендуем VPS:

### Бюджетные варианты:

**1. [Hetzner Cloud](https://www.hetzner.com/cloud)** 🇩🇪
- От €4.15/месяц (CX11)
- Отличная производительность
- Дата-центры в Европе

**2. [DigitalOcean](https://www.digitalocean.com)** 🇺🇸
- От $4/месяц (Basic Droplet)
- Простой интерфейс
- Много туториалов

**3. [Vultr](https://www.vultr.com)** 🌍
- От $2.50/месяц
- Много локаций

**4. [Contabo](https://contabo.com)** 🇩🇪
- От €4/месяц
- Много ресурсов за цену

### Настройка VPS:

После создания сервера:

```bash
# 1. Подключитесь по SSH
ssh root@your-server-ip

# 2. Обновите систему
apt update && apt upgrade -y

# 3. Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# 4. Скопируйте проект
git clone <your-repo>
cd arctBot

# 5. Создайте .env и добавьте credentials
nano .env  # добавьте BOT_TOKEN

# Загрузите google_credentials.json через SCP:
# scp config/google_credentials.json root@your-server-ip:/root/arctBot/config/

# 6. Запустите
docker-compose up -d

# 7. Проверьте логи
docker-compose logs -f
```

Подробнее см. [DEPLOYMENT.md](DEPLOYMENT.md)

---

## Сравнение платформ

| Платформа | Цена | Легкость | Файлы | Логи | Авто-деплой |
|-----------|------|----------|-------|------|-------------|
| **Render.com** | Бесплатно | ⭐⭐⭐⭐⭐ | ✅ | ✅ | ✅ |
| **Heroku** | От $7/мес | ⭐⭐⭐⭐ | ❌ | ✅ | ✅ |
| **Railway** | $5 кредитов | ⭐⭐⭐⭐ | ❌ | ✅ | ✅ |
| **VPS** | От $2/мес | ⭐⭐⭐ | ✅ | ✅ | ⚙️ |

---

## Рекомендации

### Для новичков:
👉 **Render.com** - самый простой и бесплатный вариант

### Для опытных:
👉 **VPS (Hetzner)** - полный контроль и больше возможностей

### Для тестирования:
👉 **Railway.app** - быстрый старт

---

## ⚠️ Важно для облачных платформ

1. **Google Credentials**: Большинство облачных платформ не поддерживают загрузку файлов напрямую. Render.com - исключение.

2. **Бесплатные планы**: Могут "засыпать" при неактивности. Бот может не отвечать мгновенно.

3. **Регион**: Выбирайте ближайший к вам регион для лучшей производительности.

4. **Логи**: Обязательно настройте просмотр логов для отладки.

---

## 🆘 Помощь

Если платформа не поддерживает загрузку `google_credentials.json`:

**Вариант 1**: Используйте Render.com (поддерживает Secret Files)

**Вариант 2**: Используйте VPS с Docker

**Вариант 3**: Храните credentials в переменной окружения (небезопасно):
```python
# В config/settings.py можно добавить:
import json
import os

if os.getenv('GOOGLE_CREDENTIALS_JSON'):
    # Создать файл из переменной окружения
    with open(GOOGLE_CREDENTIALS_PATH, 'w') as f:
        f.write(os.getenv('GOOGLE_CREDENTIALS_JSON'))
```

---

**Успешного деплоя!** 🚀
