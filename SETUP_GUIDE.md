# 📋 Руководство по настройке бота

## Шаг 1: Установка Python и зависимостей

### 1.1 Проверьте версию Python
```bash
python --version
```
Требуется Python 3.9 или выше.

### 1.2 Создайте виртуальное окружение
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 1.3 Установите зависимости
```bash
pip install -r requirements.txt
```

---

## Шаг 2: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Поиск участников")
4. Введите username бота (например: "my_participants_bot")
5. **Скопируйте токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Шаг 3: Настройка Google Sheets API

### 3.1 Создайте проект в Google Cloud

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите **"Create Project"** (Создать проект)
3. Введите название проекта (например: "Telegram Bot Sheets")
4. Нажмите **"Create"**

### 3.2 Включите Google Sheets API

1. В левом меню выберите **"APIs & Services"** → **"Library"**
2. Найдите **"Google Sheets API"**
3. Нажмите на неё и затем **"Enable"** (Включить)

### 3.3 Создайте Service Account

1. В левом меню выберите **"APIs & Services"** → **"Credentials"**
2. Нажмите **"Create Credentials"** → **"Service Account"**
3. Введите название (например: "telegram-bot-service")
4. Нажмите **"Create and Continue"**
5. В поле **"Role"** выберите **"Viewer"** (Просмотр)
6. Нажмите **"Continue"** → **"Done"**

### 3.4 Создайте ключ для Service Account

1. На странице **"Credentials"** найдите созданный Service Account
2. Нажмите на него (откроется страница деталей)
3. Перейдите на вкладку **"Keys"** (Ключи)
4. Нажмите **"Add Key"** → **"Create new key"**
5. Выберите формат **JSON**
6. Нажмите **"Create"**
7. **Файл автоматически скачается** (например: `your-project-123456-abc123.json`)

### 3.5 Сохраните файл credentials

1. Переименуйте скачанный файл в `google_credentials.json`
2. Переместите его в папку `config/` вашего проекта
3. Путь должен быть: `d:\arctBot\config\google_credentials.json`

### 3.6 Предоставьте доступ к таблице

1. Откройте файл `config/google_credentials.json`
2. Найдите строку `"client_email"` (выглядит так: `"название@project-id.iam.gserviceaccount.com"`)
3. **Скопируйте этот email**
4. Откройте вашу [Google Таблицу](https://docs.google.com/spreadsheets/d/1YYvqtrrEG2ssNLbKnsIX3goVQfpeJ-E8wcM06P2ts7Q/edit)
5. Нажмите кнопку **"Share"** (Настройки доступа) в правом верхнем углу
6. Вставьте скопированный email
7. Убедитесь, что выбран доступ **"Viewer"** (Читатель)
8. Снимите галочку **"Notify people"** (не нужно отправлять уведомление)
9. Нажмите **"Share"**

---

## Шаг 4: Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

2. Откройте файл `.env` в текстовом редакторе

3. Замените `your_telegram_bot_token_here` на ваш **токен от BotFather**

Пример:
```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

4. Остальные параметры уже настроены (ID таблицы уже указан)

---

## Шаг 5: Запуск бота

### 5.1 Убедитесь, что виртуальное окружение активировано
```bash
# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 5.2 Запустите бота
```bash
python main.py
```

### 5.3 Ожидаемый вывод
```
INFO: Logging system initialized
INFO: Testing Google Sheets connection...
INFO: Successfully connected to Google Sheets API
INFO: Successfully loaded XX records from spreadsheet
INFO: Bot is starting polling...
INFO: Press Ctrl+C to stop the bot
```

---

## Шаг 6: Тестирование бота

1. Откройте Telegram
2. Найдите вашего бота по username (который вы создали в BotFather)
3. Нажмите **"Start"** или отправьте `/start`
4. Нажмите кнопку **"🔍 Начать поиск"**
5. Выберите поле для поиска (например, "Фамилия")
6. Введите значение (например, фамилию участника)
7. Получите результаты!

---

## ⚠️ Возможные проблемы и решения

### Ошибка: "BOT_TOKEN is not set"
**Решение:** Проверьте, что файл `.env` создан и содержит правильный токен бота.

### Ошибка: "Credentials file not found"
**Решение:** Убедитесь, что файл `google_credentials.json` находится в папке `config/`.

### Ошибка: "Failed to connect to Google Sheets"
**Решение:** 
1. Проверьте, что Google Sheets API включён в вашем проекте
2. Проверьте, что Service Account имеет доступ к таблице (см. Шаг 3.6)

### Ошибка: "No data found in spreadsheet"
**Решение:** Убедитесь, что таблица не пустая и содержит заголовки в первой строке.

### Поиск ничего не находит
**Решение:** 
1. Проверьте, что вы вводите ТОЧНОЕ значение (регистр не важен)
2. Проверьте, что в таблице есть такое значение
3. Убедитесь, что названия столбцов совпадают с ожидаемыми:
   - "Фамилия"
   - "Имя"
   - "Отчество"
   - "Класс"
   - "ID участника"
   - "Предметы"

---

## 📝 Примечания

- Бот кэширует данные из таблицы при первом запросе
- Если данные в таблице изменились, перезапустите бота
- Логи сохраняются в файл `logs/bot.log`
- Для остановки бота нажмите `Ctrl+C`

---

## 🆘 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи в файле `logs/bot.log`
2. Убедитесь, что все шаги выполнены правильно
3. Проверьте, что все зависимости установлены: `pip list`

Удачи! 🚀
